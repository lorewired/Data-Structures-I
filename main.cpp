#include "loras_lib.cpp"
using namespace std;

int main() {
    int n, k; cin >> n;

    loras::linked_list<int> v;
    for (int i = 0; i < n; i++) {
        v.(i+1);
    }
    v.cout();

    return 0;
}